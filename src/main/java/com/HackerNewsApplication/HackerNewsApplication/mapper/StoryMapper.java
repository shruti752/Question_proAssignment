package com.HackerNewsApplication.HackerNewsApplication.mapper;

import com.HackerNewsApplication.HackerNewsApplication.Dao.StoryDto;
import com.HackerNewsApplication.HackerNewsApplication.entity.Story;

public class StoryMapper {
  /*  public static StoryDto mapToStoryDao(Story story){
StoryDto storyDto = new StoryDto(
        story.getId(),
        story.getTitle(),
        story.getUrl(),
        story.getScore(),
        story.getSubmissionTime(),
        story.getUser(),
        story.getChildComments()
);
return storyDto;
    }

    public static Story mapToStory(StoryDto storyDto){
        Story story = new Story(
                storyDto.getId(),
                storyDto.getTitle(),
                storyDto.getUrl(),
                storyDto.getScore(),
                storyDto.getSubmissionTime(),
                storyDto.getUser(),
                storyDto.getChildComments()
        );
        return story;
    }*/

}
